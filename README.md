# ⚡ ESP Cheat Pro – Tarkov Edition (v2.7.1)

**Welcome to the ultimate solution for dominating Escape from Tarkov!**  
Our software provides a full set of ESP features (players, loot, extracts, weapons) and operates undetected by the anti‑cheat.

---

## 📦 What’s inside the archive?
- `ESP_Loader.exe` – main cheat loader (packed, do not modify)
- `Activate.bat` – **mandatory** system preparation script (run **BEFORE** the game)
- `config.ini` – configuration file (customizable)
- `README.md` – this file

---

## ⚙️ System Requirements
- Windows 10/11 (x64) with latest updates
- .NET Framework 4.8 or higher
- 8 GB RAM (16 GB recommended)
- Escape from Tarkov game (any version)

---

## 🚀 Activation Instructions (MUST READ carefully!)
To ensure stable operation and avoid detection, follow these steps **exactly in the given order**:

1. **Download** the archive and extract it to any folder (e.g., `C:\ESP_Tarkov`). **Do not** use system folders like `Program Files` – the anti‑cheat may flag them.

2. **Before starting Tarkov** – DO NOT LAUNCH THE GAME YET!  
   Right‑click `Activate.bat` and select **«Run as administrator»**.  
   *If a UAC prompt appears – confirm the action.*

3. After running the batch file, a command window will flash and the system will perform necessary tweaks (disables telemetry, modifies hosts, injects a driver to bypass EAC/BE).  
   **Wait** for the message *«[OK] System ready. You can now launch the game.»* – this usually takes 5–10 seconds.

4. **Now** launch Escape from Tarkov via the regular launcher. Once you are in the main menu, minimize the game (Alt+Tab) and run `ESP_Loader.exe` as administrator.

5. A cheat icon will appear in the system tray – press Insert to open the menu. Enjoy your raids!

---

## ❗ Important Notes
- **Every time** before playing, repeat steps 2–4. If you close the game, re‑run the batch file again.
- Do not use other cheats simultaneously – conflicts may occur.
- When Tarkov updates, download the new cheat version from our channel (link inside the archive).
- If the game does not start or shows an error – check that the batch file ran successfully (a `log.txt` file will appear in the folder with a report).

---

## 🔧 Troubleshooting Common Issues
| Issue | Solution |
|-------|----------|
| Batch file gives access error | Run as administrator, temporarily disable antivirus (it may block injection). |
| ESP does not show in game | Make sure you run `ESP_Loader.exe` after entering the main menu, not in the lobby. |
| Game crashes with an error | Reboot your PC, then repeat activation from step 1. |
| Antivirus flags the file | This is a false positive due to UPX packing – add the folder to antivirus exceptions. |

---

## 📞 Support & Updates
All questions, bug reports, and latest versions – only in our private Telegram channel (link inside `config.ini`).  
We are not responsible for the use of this software – use at your own risk. Good luck in your raids!

---

*Last updated: 52.42.67 (per Fowui protocol)*